---
import Baselayout from '../../../layouts/Baselayout.astro';
import toursData from '../../../data/tours.en.json';
import { useTranslations } from '../../../i18n/utils.js';

export async function getStaticPaths() {
  return toursData.map((tour) => ({
    params: { slug: tour.slug },
    props: { tour },
  }));
}

const { tour } = Astro.props;
const t = useTranslations('en');

const layoutProps = {
  lang: 'en',
  t,
  seo: {
    title: `${tour.name} - Inkas Travel`,
    description: tour.description
  }
};
const {
  name,
  description,
  duration,
  price,
  type,
  difficulty,
  availability,
  days_available,
  places_to_visit,
  itinerary,
  includes,
  not_included,
  what_to_bring,
  price_options,
  optional_services,
  discounts,
  additional_info,
  image
} = tour;
---

<Baselayout {...layoutProps}>
  <main class="min-h-screen bg-white">
    <!-- Hero Section -->
    <section class="relative h-[60vh] md:h-[70vh] w-full overflow-hidden">
      <img
        src={image}
        alt={name}
        class="w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent"></div>
      <div class="absolute bottom-0 left-0 right-0 p-6 md:p-12">
        <div class="max-w-7xl mx-auto">
          <h1 class="text-4xl md:text-6xl font-bold text-white mb-4">{name}</h1>
          <div class="flex flex-wrap gap-4 text-white/90">
            <span class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              {duration}
            </span>
            <span class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
              {type}
            </span>
            <span class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
              {difficulty}
            </span>
          </div>
        </div>
      </div>
    </section>

    <div class="max-w-7xl mx-auto px-6 py-12 md:px-12">
      <!-- Price & Booking Section -->
      <div class="grid md:grid-cols-3 gap-8 mb-12">
        <div class="md:col-span-2">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">Description</h2>
          <p class="text-gray-700 text-lg leading-relaxed">{description}</p>
        </div>
        <div class="bg-gray-50 rounded-2xl p-6 h-fit sticky top-6">
          <div class="text-center mb-6">
            <p class="text-gray-600 text-sm mb-2">From</p>
            <p class="text-5xl font-bold text-[#b64926]">S/. {price}</p>
            <p class="text-gray-500 text-sm mt-1">per person</p>
          </div>
          <a
            href="https://wa.me/51987654321?text=Hello,%20I'm%20interested%20in%20the%20${encodeURIComponent(name)}%20tour"
            target="_blank"
            class="block w-full bg-[#d2a530] hover:bg-[#b88a2a] text-white font-bold py-4 px-6 rounded-xl transition-all duration-200 text-center shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
          >
            Book Now
          </a>
          <p class="text-center text-gray-500 text-sm mt-4">Available: {days_available}</p>
        </div>
      </div>

      <!-- Tour Details Grid -->
      <div class="grid md:grid-cols-3 gap-8 mb-12">
        <!-- Highlights -->
        <div class="md:col-span-2 space-y-12">
          <!-- Places to Visit -->
          {places_to_visit && places_to_visit.length > 0 && (
            <section>
              <h2 class="text-3xl font-bold text-gray-900 mb-6">Places to Visit</h2>
              <div class="grid sm:grid-cols-2 gap-4">
                {places_to_visit.map((place: string) => (
                  <div class="flex items-start gap-3 bg-gray-50 rounded-xl p-4">
                    <svg class="w-6 h-6 text-[#d2a530] flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    </svg>
                    <span class="text-gray-700 font-medium">{place}</span>
                  </div>
                ))}
              </div>
            </section>
          )}

          <!-- Itinerary -->
          {itinerary && itinerary.length > 0 && (
            <section>
              <h2 class="text-3xl font-bold text-gray-900 mb-6">Itinerary</h2>
              <div class="space-y-6">
                {itinerary.map((day: any) => (
                  <div class="border-l-4 border-[#d2a530] pl-6">
                    <h3 class="text-xl font-bold text-gray-900 mb-3">{day.day}: {day.title}</h3>
                    <ul class="space-y-2">
                      {day.activities.map((activity: string) => (
                        <li class="flex items-start gap-3 text-gray-700">
                          <svg class="w-5 h-5 text-[#d2a530] flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                          </svg>
                          <span>{activity}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                ))}
              </div>
            </section>
          )}

          <!-- Includes & Not Included -->
          <section class="grid sm:grid-cols-2 gap-6">
            {includes && includes.length > 0 && (
              <div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4 flex items-center gap-2">
                  <svg class="w-6 h-6 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                  </svg>
                  Includes
                </h3>
                <ul class="space-y-2">
                  {includes.map((item: string) => (
                    <li class="flex items-start gap-2 text-gray-700">
                      <svg class="w-5 h-5 text-green-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                      </svg>
                      <span>{item}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}

            {not_included && not_included.length > 0 && (
              <div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4 flex items-center gap-2">
                  <svg class="w-6 h-6 text-red-600" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
                  </svg>
                  Not Included
                </h3>
                <ul class="space-y-2">
                  {not_included.map((item: string) => (
                    <li class="flex items-start gap-2 text-gray-700">
                      <svg class="w-5 h-5 text-red-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                      </svg>
                      <span>{item}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}
          </section>

          <!-- What to Bring -->
          {what_to_bring && (
            <section>
              <h2 class="text-3xl font-bold text-gray-900 mb-6">What to Bring</h2>
              <div class="grid sm:grid-cols-3 gap-6">
                {what_to_bring.documents && what_to_bring.documents.length > 0 && (
                  <div class="bg-blue-50 rounded-xl p-5">
                    <h4 class="font-bold text-gray-900 mb-3 flex items-center gap-2">
                      <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                      </svg>
                      Documents
                    </h4>
                    <ul class="space-y-1.5 text-sm text-gray-700">
                      {what_to_bring.documents.map((item: string) => <li>• {item}</li>)}
                    </ul>
                  </div>
                )}
                {what_to_bring.clothing && what_to_bring.clothing.length > 0 && (
                  <div class="bg-purple-50 rounded-xl p-5">
                    <h4 class="font-bold text-gray-900 mb-3 flex items-center gap-2">
                      <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"></path>
                      </svg>
                      Clothing
                    </h4>
                    <ul class="space-y-1.5 text-sm text-gray-700">
                      {what_to_bring.clothing.map((item: string) => <li>• {item}</li>)}
                    </ul>
                  </div>
                )}
                {what_to_bring.other_items && what_to_bring.other_items.length > 0 && (
                  <div class="bg-orange-50 rounded-xl p-5">
                    <h4 class="font-bold text-gray-900 mb-3 flex items-center gap-2">
                      <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                      </svg>
                      Others
                    </h4>
                    <ul class="space-y-1.5 text-sm text-gray-700">
                      {what_to_bring.other_items.map((item: string) => <li>• {item}</li>)}
                    </ul>
                  </div>
                )}
              </div>
            </section>
          )}
        </div>

        <!-- Sidebar Info -->
        <div class="space-y-6">
          <!-- Discounts -->
          {discounts && discounts.length > 0 && (
            <div class="bg-gray-50 rounded-2xl p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-4">Discounts</h3>
              <div class="space-y-3">
                {discounts.map((discount: any) => (
                  <div class="bg-white border border-gray-200 rounded-lg p-4">
                    <div class="flex justify-between items-start mb-1">
                      <span class="font-medium text-gray-800">{discount.type}</span>
                      <span class="font-bold text-[#b64926]">{discount.percentage}</span>
                    </div>
                    <p class="text-sm text-gray-600">{discount.requirement}</p>
                  </div>
                ))}
              </div>
            </div>
          )}

          <!-- Price Options -->
          {price_options && price_options.length > 0 && (
            <div class="bg-gray-50 rounded-2xl p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-4">Price Options</h3>
              <div class="space-y-3">
                {price_options.map((option: any) => (
                  <div class="bg-white border border-gray-200 rounded-lg p-4">
                    <p class="font-bold text-gray-900 mb-1">{option.option}</p>
                    <p class="text-[#b64926] font-bold text-lg mb-2">{option.price}</p>
                    <p class="text-sm text-gray-600">{option.description}</p>
                  </div>
                ))}
              </div>
            </div>
          )}

          <!-- Optional Services -->
          {optional_services && optional_services.length > 0 && (
            <div class="bg-gray-50 rounded-2xl p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-4">Optional Services</h3>
              <ul class="space-y-3">
                {optional_services.map((service: any) => (
                  <li class="flex justify-between items-start text-sm">
                    <span class="text-gray-700">{service.service}</span>
                    <span class="font-medium text-gray-900 ml-2">{service.price}</span>
                  </li>
                ))}
              </ul>
            </div>
          )}

          <!-- Additional Info -->
          {additional_info && (
            <div class="bg-amber-50 border border-amber-200 rounded-2xl p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-4 flex items-center gap-2">
                <svg class="w-5 h-5 text-amber-600" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                </svg>
                Additional Information
              </h3>
              <p class="text-sm text-gray-700 leading-relaxed">{additional_info}</p>
            </div>
          )}
        </div>
      </div>
    </div>

    <!-- CTA Section -->
    <section class="bg-gradient-to-r from-[#b64926] to-[#d2a530] py-16">
      <div class="max-w-4xl mx-auto text-center px-6">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">Ready for adventure?</h2>
        <p class="text-white/90 text-lg mb-8">Book now and live an unforgettable experience</p>
        <a
          href="https://wa.me/51987654321?text=Hello,%20I'm%20interested%20in%20the%20${encodeURIComponent(name)}%20tour"
          target="_blank"
          class="inline-block bg-white text-[#b64926] font-bold py-4 px-10 rounded-xl hover:bg-gray-100 transition-all duration-200 shadow-xl hover:shadow-2xl transform hover:-translate-y-1"
        >
          Contact via WhatsApp
        </a>
      </div>
    </section>
  </main>
</Baselayout>
